<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width">

	<title>{ifset title}{include title|stripHtml} | {/ifset}User Login Example</title>

	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
</head>

<body>
	<div class=container>
		<div n:foreach="$flashes as $flash" n:class="alert, 'alert-' . $flash->type">{$flash->message}</div>

	{if $user->isLoggedIn()} 
		<img src="/{$user->getIdentity()->image}" alt="Girl in a jacket" width="250" height="250">  
		<p> ({$user->getIdentity()->username}) | (<a n:href="User:edit">Edit User</a>) | (<a n:href="Sign:out">Sign Out</a>)</p>
	{else}
		<p>(<a n:href="Sign:in">Sign In</a>)</p>
	{/if}

		{include content}
		<p>(<a n:href="User:default">User Default</a>)</p>



	</div>

	{block scripts}
	<script src="https://nette.github.io/resources/js/3/netteForms.min.js"></script>
	{/block}
</body>
</html>
